//Build a simple loop, inside a function, that takes an array and prints the value of the array to the console
function numArray() {
    const arr=[ "Hannah","Tannah","Samara","Amara"];
    for (let i = 0; i < arr.length; i++) {
       return arr;
    }
}
console.log(numArray());
